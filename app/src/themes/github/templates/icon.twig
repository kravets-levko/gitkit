{% spaceless %}
<svg class="octicon {{ class }}" width="{{ width ?? size ?? 16 }}" height="{{ height ?? size ?? 16 }}">
  <use xlink:href="{{ assetsPath }}/{{ iconset ?? 'octicons' }}.svg#{{ icon | escape('url') }}"></use>
</svg>
{% endspaceless %}
