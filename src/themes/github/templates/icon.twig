{% spaceless %}
<svg class="octicon" width="{{ width ?? size ?? 16 }}" height="{{ height ?? size ?? 16 }}">
  <use xlink:href="{{ assetsPath }}/octicons.svg#{{ icon | escape('url') }}"></use>
</svg>
{% endspaceless %}
